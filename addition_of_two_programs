#include <iostream>

int main()
{
    int first, second, sum;
    
    cout << "Enter two integers: ";
    cin >> first; 
    cin >> second;

    sum = first + second;

    cout << first << " + " <<  second << " = " << sum;     

    return 0;
}
